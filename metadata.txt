[general]
name=Area Length Buttons
qgisMinimumVersion=3.0
description=Adds Area and Length buttons to the attribute table toolbar. Click to add a virtual field with a chosen unit (m2/km2 or m/km). Buttons auto enable/disable based on layer geometry type.
version=0.4
author=Safin Lin
email=tszyilin@gmail.com
about=Adds two buttons to every attribute table toolbar. Area button (polygon layers) adds a virtual area field in m2 or km2. Length button (line layers) adds a virtual length field in m or km. Uses ellipsoidal measurement so geographic (lat/lon) layers also produce correct metric values.
tracker=https://example.com/issues
repository=https://example.com/repo
category=Vector
experimental=True
deprecated=False
